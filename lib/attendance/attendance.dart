import 'package:flutter/material.dart';

class AttendanceTracker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Attendance Here'),);
  }
}
