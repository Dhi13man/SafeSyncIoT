import 'package:flutter/material.dart';

class EmployeeStatistics extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Statistics here.'),);
  }
}
